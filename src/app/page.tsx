import WebLayout from "./(web)/layout";
import WebRoot from "./(web)/(pages)/(root)/page";

function Root() {
  return (
    <WebLayout>
      <WebRoot />
    </WebLayout>
  );
}

export default Root
